server:
  servlet:
    contextPath: /assignment

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/SIGHT
    username: root
    password:
    driver:
      class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
